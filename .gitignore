# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env*
!.env.example
!.env.production.example
!.env.edge.example
!.env.backup.example
!.env.monitor.example
db/leads-db.json
backups/
tmp/
test-results/
playwright-report/
blob-report/
playwright/.cache/
*.age
*.pem
*.key

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Broader env-file protection (keep .env.example tracked)
.env.local
.env.*.local

# Windows OS cruft
Thumbs.db
desktop.ini

.sentry-local.log
.neon

# Design working files: mockups, image sources and design-tool state. They are
# kept on disk and stay recoverable from history (git show <rev>:<path>), but
# they are never deployed and were adding ~43 MB to every clone.
design-proposals/
source-assets/
.impeccable/

# Local tooling / workspace files
.mcp.json
.obsidian/
graphify-out/
