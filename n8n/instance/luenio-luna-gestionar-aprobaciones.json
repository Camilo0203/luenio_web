{
  "name": "Luenio LUNA | Gestionar aprobaciones",
  "active": true,
  "nodes": [
    {
      "id": "ap-trigger",
      "name": "01 Entrada | Comando privado",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ],
      "parameters": {
        "inputSource": "workflowInputs",
        "workflowInputs": {
          "values": [
            {
              "name": "content",
              "type": "string"
            },
            {
              "name": "private_note",
              "type": "boolean"
            },
            {
              "name": "human_sender",
              "type": "boolean"
            },
            {
              "name": "sender_is_bot",
              "type": "boolean"
            },
            {
              "name": "hmac_valid",
              "type": "boolean"
            },
            {
              "name": "sender_id",
              "type": "string"
            },
            {
              "name": "environment",
              "type": "string"
            },
            {
              "name": "source_hash",
              "type": "string"
            }
          ]
        }
      }
    },
    {
      "id": "ap-parse",
      "name": "02 Seguridad | Validar comando humano",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        270,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const PROPOSAL_STATUSES = new Set(['none','requested','rendering','pending_approval','approved','rejected','sending','partially_sent','sent','failed','blocked_after_send_failure']);\nconst APPOINTMENT_STATUSES = new Set(['requested','availability_checked','pending_confirmation','pending_approval','confirmed','cancelled','expired','failed']);\n\nfunction text(value) { return String(value == null ? '' : value).replace(/\\s+/g, ' ').trim(); }\nfunction bool(value) { return value === true || value === 1 || /^(true|1|yes|si)$/i.test(text(value)); }\nfunction validEmail(value) { return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(text(value)); }\nfunction stableHash(value) {\n  let h1 = 0xdeadbeef ^ text(value).length;\n  let h2 = 0x41c6ce57 ^ text(value).length;\n  for (let index = 0; index < text(value).length; index += 1) {\n    const code = text(value).charCodeAt(index);\n    h1 = Math.imul(h1 ^ code, 2654435761);\n    h2 = Math.imul(h2 ^ code, 1597334677);\n  }\n  h1 = Math.imul(h1 ^ (h1 >>> 16), 2246822507) ^ Math.imul(h2 ^ (h2 >>> 13), 3266489909);\n  h2 = Math.imul(h2 ^ (h2 >>> 16), 2246822507) ^ Math.imul(h1 ^ (h1 >>> 13), 3266489909);\n  return (4294967296 * (2097151 & h2) + (h1 >>> 0)).toString(16).padStart(14, '0');\n}\n\nfunction sanitizeProposalSnapshot(context, catalog) {\n  const allowedContext = ['business_type','project_goal','pain_point','current_process','impact','desired_outcome','discovery_urgency','recommended_solution','selected_solution','solution_confirmed','conversation_summary'];\n  const snapshot = { schema_version: '3.0', context: {}, catalog: { version: text(catalog && catalog.version), packages: {} } };\n  for (const key of allowedContext) snapshot.context[key] = typeof context?.[key] === 'boolean' ? context[key] : text(context?.[key]);\n  const packages = catalog?.catalog_packages || {};\n  for (const key of ['PKG-ESSENTIAL','PKG-PROFESSIONAL']) {\n    const row = packages[key] || {};\n    snapshot.catalog.packages[key] = { name: text(row.name), price_project: Number(row.price_project || 0), price_monthly: Number(row.price_monthly || 0) };\n  }\n  return snapshot;\n}\n\nfunction buildProposalRequest(input) {\n  const context = input?.context || {};\n  const catalog = input?.catalog || {};\n  const errors = [];\n  if (!bool(context.discovery_done)) errors.push('discovery_incomplete');\n  if (!bool(context.solution_confirmed)) errors.push('solution_unconfirmed');\n  if (!bool(context.proposal_requested)) errors.push('proposal_not_requested');\n  if (!validEmail(context.email)) errors.push('email_invalid');\n  if (catalog.catalog_ready !== true) errors.push('catalog_unavailable');\n  const snapshot = sanitizeProposalSnapshot(context, catalog);\n  const snapshot_json = JSON.stringify(snapshot);\n  const requestKey = `${text(context.conversation_id)}|${text(input.message_id)}|proposal`;\n  return {\n    ok: errors.length === 0,\n    proposal_id: `prop_${stableHash(requestKey)}`,\n    request_key: requestKey,\n    conversation_id: text(context.conversation_id),\n    message_id: text(input.message_id),\n    status: errors.length ? 'failed' : 'requested',\n    snapshot_json,\n    snapshot_hash: stableHash(snapshot_json),\n    recipient_email_hash: '',\n    catalog_version: text(catalog.version),\n    proposal_version: 1,\n    requested_at: text(input.now || new Date().toISOString()),\n    updated_at: text(input.now || new Date().toISOString()),\n    environment: text(input.environment || 'staging'),\n    source_version: text(input.source_version || '3.0'),\n    last_error_code: errors.join(','),\n  };\n}\n\nfunction parseApprovalCommand(input) {\n  const body = text(input?.content || input?.message);\n  const privateNote = bool(input?.private_note || input?.private);\n  const human = bool(input?.human_sender) && !bool(input?.sender_is_bot);\n  const hmac = bool(input?.hmac_valid);\n  const match = body.match(/^\\/luna\\s+(aprobar|rechazar)\\s+(propuesta|cita)\\s+([a-z0-9_-]+)(?:\\s+(.+))?$/i);\n  if (!hmac) return { ok: false, reason_code: 'INVALID_SIGNATURE' };\n  if (!privateNote) return { ok: false, reason_code: 'COMMAND_NOT_PRIVATE' };\n  if (!human) return { ok: false, reason_code: 'COMMAND_SENDER_NOT_HUMAN' };\n  if (!match) return { ok: false, reason_code: 'COMMAND_INVALID' };\n  const action = match[1].toLowerCase();\n  const request_type = match[2].toLowerCase() === 'propuesta' ? 'proposal' : 'appointment';\n  if (action === 'rechazar' && !text(match[4])) return { ok: false, reason_code: 'REJECTION_REASON_REQUIRED' };\n  return { ok: true, action: action === 'aprobar' ? 'approve' : 'reject', request_type, request_id: match[3], reason: text(match[4]), reason_code: 'APPROVAL_COMMAND_VALID' };\n}\n\nfunction validateMedia(input) {\n  const mime = text(input?.mime_type).toLowerCase();\n  const bytes = Number(input?.size_bytes || 0);\n  const duration = Number(input?.duration_seconds || 0);\n  const audio = ['audio/ogg','audio/mpeg','audio/mp4','audio/webm'].includes(mime);\n  const image = ['image/jpeg','image/png','image/webp'].includes(mime);\n  if (!audio && !image) return { ok: false, kind: 'unsupported', reason_code: 'MEDIA_MIME_UNSUPPORTED' };\n  if (audio && bytes > 10 * 1024 * 1024) return { ok: false, kind: 'audio', reason_code: 'MEDIA_TOO_LARGE' };\n  if (audio && duration > 300) return { ok: false, kind: 'audio', reason_code: 'MEDIA_TOO_LONG' };\n  if (image && bytes > 8 * 1024 * 1024) return { ok: false, kind: 'image', reason_code: 'MEDIA_TOO_LARGE' };\n  return { ok: true, kind: audio ? 'audio' : 'image', reason_code: 'MEDIA_ACCEPTED' };\n}\n\nfunction validateAppointment(input) {\n  const context = input?.context || {};\n  const status = text(input?.status || 'requested');\n  const errors = [];\n  if (!APPOINTMENT_STATUSES.has(status)) errors.push('appointment_status_invalid');\n  if (!validEmail(context.email)) errors.push('email_invalid');\n  if (!bool(input?.consent_explicit)) errors.push('consent_required');\n  if (text(input?.timezone || 'America/Bogota') !== 'America/Bogota') errors.push('timezone_invalid');\n  return { ok: errors.length === 0, status: errors.length ? 'failed' : status, duration_minutes: 30, buffer_minutes: 15, timezone: 'America/Bogota', reason_code: errors.join(',') || 'APPOINTMENT_VALID' };\n}\n\nfunction transitionProposal(current, event) {\n  const status = PROPOSAL_STATUSES.has(text(current)) ? text(current) : 'none';\n  const transitions = {\n    none: { request: 'requested' }, requested: { render: 'rendering', fail: 'failed' }, rendering: { rendered: 'pending_approval', fail: 'failed' },\n    pending_approval: { approve: 'approved', reject: 'rejected' }, approved: { send: 'sending' }, sending: { partial: 'partially_sent', delivered: 'sent', fail_after_send: 'blocked_after_send_failure', fail: 'failed' },\n    partially_sent: { delivered: 'sent', fail_after_send: 'blocked_after_send_failure' }, rejected: {}, sent: {}, failed: {}, blocked_after_send_failure: {},\n  };\n  const next = transitions[status]?.[text(event)];\n  return { ok: Boolean(next), previous_status: status, status: next || status, proposal_sent_this_turn: next === 'sent' && status !== 'sent', reason_code: next ? 'PROPOSAL_TRANSITION_OK' : 'PROPOSAL_TRANSITION_BLOCKED' };\n}\n\n\nconst input=$input.first()?.json||{}; const command=parseApprovalCommand(input); return [{json:{...command,sender_id:String(input.sender_id||''),environment:String(input.environment||'staging'),source_hash:String(input.source_hash||'')}}];"
      }
    },
    {
      "id": "ap-valid",
      "name": "03 Filtro | ¿Comando válido?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        540,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 3
          },
          "conditions": [
            {
              "id": "condition",
              "leftValue": "={{ String($json.ok) }}",
              "rightValue": "true",
              "operator": {
                "type": "string",
                "operation": "equals",
                "name": "filter.operator.equals"
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      }
    },
    {
      "id": "ap-ignore",
      "name": "04 Salida | Comando ignorado",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        810,
        -180
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const x=$input.first()?.json||{}; return [{json:{ok:false,status:'ignored',reason_code:String(x.reason_code||'COMMAND_INVALID'),environment:String(x.environment||'staging'),source_hash:String(x.source_hash||'')}}];"
      }
    },
    {
      "id": "ap-kind",
      "name": "05 Filtro | ¿Es propuesta?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        810,
        80
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 3
          },
          "conditions": [
            {
              "id": "condition",
              "leftValue": "={{ String($json.request_type) }}",
              "rightValue": "proposal",
              "operator": {
                "type": "string",
                "operation": "equals",
                "name": "filter.operator.equals"
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      }
    },
    {
      "id": "ap-prop-get",
      "name": "06 Propuesta | Buscar solicitud",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        1080,
        0
      ],
      "alwaysOutputData": true,
      "parameters": {
        "operation": "get",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "MAaI6cmQOlZ5S54c",
          "cachedResultName": "luenio_proposal_requests"
        },
        "matchType": "allConditions",
        "filters": {
          "conditions": [
            {
              "keyName": "proposal_id",
              "keyValue": "={{ $('02 Seguridad | Validar comando humano').first().json.request_id }}"
            }
          ]
        },
        "limit": 2
      }
    },
    {
      "id": "ap-appt-get",
      "name": "07 Cita | Buscar solicitud",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        1080,
        170
      ],
      "alwaysOutputData": true,
      "parameters": {
        "operation": "get",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "iZ8SarZJDKEMXpka",
          "cachedResultName": "luenio_appointment_requests"
        },
        "matchType": "allConditions",
        "filters": {
          "conditions": [
            {
              "keyName": "appointment_id",
              "keyValue": "={{ $('02 Seguridad | Validar comando humano').first().json.request_id }}"
            }
          ]
        },
        "limit": 2
      }
    },
    {
      "id": "ap-prop-check",
      "name": "08 Propuesta | Validar pendiente",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1350,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const cmd=$('02 Seguridad | Validar comando humano').first().json||{}; const rows=$input.all().map(i=>i.json||{}).filter(r=>String(r.proposal_id||'')===String(cmd.request_id||'')); const ok=rows.length===1&&String(rows[0].status||'')==='pending_approval'; return [{json:{...cmd,ok,reason_code:ok?'PROPOSAL_APPROVAL_READY':rows.length>1?'PROPOSAL_DUPLICATE_REQUEST':rows.length===0?'PROPOSAL_NOT_FOUND':'PROPOSAL_NOT_PENDING',next_status:cmd.action==='approve'?'approved':'rejected'}}];"
      }
    },
    {
      "id": "ap-appt-check",
      "name": "09 Cita | Validar pendiente",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1350,
        170
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const cmd=$('02 Seguridad | Validar comando humano').first().json||{}; const rows=$input.all().map(i=>i.json||{}).filter(r=>String(r.appointment_id||'')===String(cmd.request_id||'')); const ok=rows.length===1&&String(rows[0].status||'')==='pending_approval'; return [{json:{...cmd,ok,reason_code:ok?'APPOINTMENT_APPROVAL_READY':rows.length>1?'APPOINTMENT_DUPLICATE_REQUEST':rows.length===0?'APPOINTMENT_NOT_FOUND':'APPOINTMENT_NOT_PENDING',next_status:cmd.action==='approve'?'confirmed':'rejected'}}];"
      }
    },
    {
      "id": "ap-prop-ok",
      "name": "10 Filtro | ¿Propuesta actualizable?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        1610,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 3
          },
          "conditions": [
            {
              "id": "condition",
              "leftValue": "={{ String($json.ok) }}",
              "rightValue": "true",
              "operator": {
                "type": "string",
                "operation": "equals",
                "name": "filter.operator.equals"
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      }
    },
    {
      "id": "ap-appt-ok",
      "name": "11 Filtro | ¿Cita actualizable?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        1610,
        170
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 3
          },
          "conditions": [
            {
              "id": "condition",
              "leftValue": "={{ String($json.ok) }}",
              "rightValue": "true",
              "operator": {
                "type": "string",
                "operation": "equals",
                "name": "filter.operator.equals"
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      }
    },
    {
      "id": "ap-prop-update",
      "name": "12 Propuesta | Registrar decisión",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        1880,
        -40
      ],
      "parameters": {
        "operation": "update",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "MAaI6cmQOlZ5S54c",
          "cachedResultName": "luenio_proposal_requests"
        },
        "matchType": "allConditions",
        "filters": {
          "conditions": [
            {
              "keyName": "proposal_id",
              "keyValue": "={{ $json.request_id }}"
            },
            {
              "keyName": "status",
              "keyValue": "pending_approval"
            }
          ]
        },
        "columns": {
          "mappingMode": "defineBelow",
          "value": {
            "status": "={{ $json.next_status }}",
            "updated_at": "={{ $now.toISO() }}",
            "approved_at": "={{ $json.action === 'approve' ? $now.toISO() : null }}",
            "approved_by": "={{ $json.action === 'approve' ? $json.sender_id : '' }}",
            "rejected_at": "={{ $json.action === 'reject' ? $now.toISO() : null }}",
            "rejection_reason_code": "={{ $json.action === 'reject' ? 'advisor_rejected' : '' }}"
          },
          "schema": []
        },
        "options": {}
      }
    },
    {
      "id": "ap-appt-update",
      "name": "13 Cita | Registrar decisión",
      "type": "n8n-nodes-base.dataTable",
      "typeVersion": 1.1,
      "position": [
        1880,
        130
      ],
      "parameters": {
        "operation": "update",
        "dataTableId": {
          "__rl": true,
          "mode": "id",
          "value": "iZ8SarZJDKEMXpka",
          "cachedResultName": "luenio_appointment_requests"
        },
        "matchType": "allConditions",
        "filters": {
          "conditions": [
            {
              "keyName": "appointment_id",
              "keyValue": "={{ $json.request_id }}"
            },
            {
              "keyName": "status",
              "keyValue": "pending_approval"
            }
          ]
        },
        "columns": {
          "mappingMode": "defineBelow",
          "value": {
            "status": "={{ $json.action === 'approve' ? 'confirmed' : 'cancelled' }}",
            "updated_at": "={{ $now.toISO() }}",
            "approved_at": "={{ $json.action === 'approve' ? $now.toISO() : null }}",
            "approved_by": "={{ $json.action === 'approve' ? $json.sender_id : '' }}",
            "rejected_at": "={{ $json.action === 'reject' ? $now.toISO() : null }}",
            "rejection_reason_code": "={{ $json.action === 'reject' ? 'advisor_rejected' : '' }}",
            "confirmed_at": "={{ $json.action === 'approve' ? $now.toISO() : null }}"
          },
          "schema": []
        },
        "options": {}
      }
    },
    {
      "id": "ap-blocked",
      "name": "14 Salida | Solicitud bloqueada",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1880,
        310
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const x=$input.first()?.json||{}; return [{json:{ok:false,status:'blocked',request_id:String(x.request_id||''),request_type:String(x.request_type||''),reason_code:String(x.reason_code||'REQUEST_BLOCKED'),environment:String(x.environment||'staging'),source_hash:String(x.source_hash||'')}}];"
      }
    },
    {
      "id": "ap-done",
      "name": "15 Salida | Decisión persistida",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2160,
        50
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const cmd=$('02 Seguridad | Validar comando humano').first().json||{}; return [{json:{ok:true,status:cmd.action==='approve'?'approved':'rejected',request_id:String(cmd.request_id||''),request_type:String(cmd.request_type||''),action_request:{type:cmd.request_type,payload:{request_id:String(cmd.request_id||''),approved:cmd.action==='approve'}},reason_code:'APPROVAL_DECISION_PERSISTED',environment:String(cmd.environment||'staging'),source_hash:String(cmd.source_hash||'')}}];"
      }
    }
  ],
  "connections": {
    "01 Entrada | Comando privado": {
      "main": [
        [
          {
            "node": "02 Seguridad | Validar comando humano",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "02 Seguridad | Validar comando humano": {
      "main": [
        [
          {
            "node": "03 Filtro | ¿Comando válido?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "03 Filtro | ¿Comando válido?": {
      "main": [
        [
          {
            "node": "05 Filtro | ¿Es propuesta?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "04 Salida | Comando ignorado",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "05 Filtro | ¿Es propuesta?": {
      "main": [
        [
          {
            "node": "06 Propuesta | Buscar solicitud",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "07 Cita | Buscar solicitud",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "06 Propuesta | Buscar solicitud": {
      "main": [
        [
          {
            "node": "08 Propuesta | Validar pendiente",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "07 Cita | Buscar solicitud": {
      "main": [
        [
          {
            "node": "09 Cita | Validar pendiente",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "08 Propuesta | Validar pendiente": {
      "main": [
        [
          {
            "node": "10 Filtro | ¿Propuesta actualizable?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "09 Cita | Validar pendiente": {
      "main": [
        [
          {
            "node": "11 Filtro | ¿Cita actualizable?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "10 Filtro | ¿Propuesta actualizable?": {
      "main": [
        [
          {
            "node": "12 Propuesta | Registrar decisión",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "14 Salida | Solicitud bloqueada",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "11 Filtro | ¿Cita actualizable?": {
      "main": [
        [
          {
            "node": "13 Cita | Registrar decisión",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "14 Salida | Solicitud bloqueada",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "12 Propuesta | Registrar decisión": {
      "main": [
        [
          {
            "node": "15 Salida | Decisión persistida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "13 Cita | Registrar decisión": {
      "main": [
        [
          {
            "node": "15 Salida | Decisión persistida",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "America/Bogota",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": true,
    "errorWorkflow": "csLvqYKmih4zi4Fq"
  },
  "staticData": null
}
