/**
 * Dashboard module barrel — metrics, health, onboarding, invitations.
 * Rendering stays in apps/admin/src/admin.js for a single authenticated shell.
 */
export { classificationLabel, stageLabel, safeText, safeNumber } from "../src/format.js";
